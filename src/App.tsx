import { css } from "@emotion/css";
import { Button } from "@/components/ui/button";

const App = () => {
  return (
    <div className={css``}>
      <Button>un bouton</Button>
    </div>
  );
};

export default App;
